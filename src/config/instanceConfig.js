module.exports.config = {
    // Prevent account signup for all users
    lockAccountSignup: false,
    // Prevent account login for all users
    lockAccountLogin: false,
    // Array of usernames that are banned
    bannedUsernames: [""],
    // Prevent server creation for all users
    lockServerCreation: false
};